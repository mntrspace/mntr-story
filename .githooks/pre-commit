#!/bin/sh
# Sensitivity gate — blocks secrets / private content from entering history.
# Personal-content override:  ALLOW_SENSITIVE=1 git commit ...
# Deliberate full bypass:     git commit --no-verify
ROOT="$(git rev-parse --show-toplevel)"
exec node "$ROOT/scripts/check-sensitive.mjs" --staged
